{% set maxArticles = 3 %}
{#{% set articles = [articles: articles.title, articles: articles.createdAt] %}#}

{% for article in articles | slice(0, maxArticles) | sort((a,b)=>b.createdAt > a.createdAt) %}
    {% include 'article/_previewArticlesHome.html.twig' %}
{% endfor %}